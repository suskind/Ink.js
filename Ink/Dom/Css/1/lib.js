Ink.createModule(
    'Ink.Dom.Css', 1,
    [],
    function() {

        return {

            addClassName: function(el, className) {
                //console.log(['TODO: Dom.Css.addClassName(', el, ', ', className, ')'].join(''));
            },

            removeClassName: function(el, className) {
                //console.log(['TODO: Dom.Css.removeClassName(', el, ', ', className, ')'].join(''));
            }

        };
    }
);
